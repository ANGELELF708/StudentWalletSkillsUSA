import java.io.*;
import java.util.*;
public class Options {
//variables
   private static ArrayList<String> options;
  private int index;
  private String pick;
//constructors
  public Options() {
     options = new ArrayList<String>();
    this.index = index;
    switch(this.pick =  pick){}; //fix this piece of garbage... unless it works somehow, y'know by some grace of god.
  }
//methods
  public void addOption(String add) {
    options.add(add);
  }
  public String getOptions() {
      String getoptions =options.toString();
       return getoptions;
      }
  }


  